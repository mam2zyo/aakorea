# Antigravity Agent Protocol

이 문서는 이 저장소에서 Antigravity AI 에이전트가 코딩 및 관리 작업을 수행할 때 반드시 지켜야 할 **필수 행동 강령(Protocol)**을 정의합니다. AI는 작업을 시작하기 전과 커밋을 진행하기 전에 항상 이 문서를 숙지하고 점검해야 합니다.

## 1. Pre-flight Check (작업 전 판단)
코드를 수정하거나 명령을 실행하기 전, 요청의 규모를 분석하고 다음 브랜치 전략을 결정하여 사용자에게 알립니다.

*   **Major Changes (주요 변경사항)**
    *   **조건**: API 엔드포인트 추가/수정/삭제, DB 스키마 변경, 복잡한 비즈니스 로직 수정 등
    *   **행동**: 절대 `develop` 브랜치에 직접 커밋하지 않습니다. `feat/`, `fix/`, `refactor/` 등의 적절한 새 브랜치를 생성한 후 작업을 시작해야 합니다.
*   **Minor Changes (단순 변경사항)**
    *   **조건**: 단순 오타 수정, 간단한 CSS 조작, 영향도가 매우 낮은 버그 수정 등
    *   **행동**: `develop` 브랜치에 직접 커밋하는 것이 허용됩니다.

## 2. Post-flight Check (커밋 전 판단)
`git commit`을 수행하기 전, 변경된 코드와 문서 간의 불일치가 없는지 확인하고 현행화합니다.

*   **API 문서 현행화**
    *   **조건**: 백엔드 컨트롤러나 프론트엔드 API 호출 규격이 변경/삭제되었는가?
    *   **행동**: `docs/01-current/api/` 하위의 관련 마크다운 문서(및 `README.md`)를 즉시 업데이트합니다.
*   **도메인 문서 현행화**
    *   **조건**: 핵심 엔티티(Entity)나 비즈니스 규칙이 변경되었는가?
    *   **행동**: `docs/domain/` 하위의 문서를 업데이트합니다.
*   **원칙**: 코드 변경 사항과 정적 문서(`docs/`)는 같은 커밋 내에서(또는 같은 PR 내에서) 항상 동기화되어야 합니다.

## 3. Communication (사용자 소통)
*   작업을 시작할 때: "이 작업은 Major(또는 Minor) 변경이므로 새 브랜치를 파겠습니다(또는 직접 커밋하겠습니다)"라고 명시합니다.
*   작업을 마칠 때: "코드 수정과 함께 관련 API/도메인 문서도 모두 업데이트했습니다"라고 확인합니다.
