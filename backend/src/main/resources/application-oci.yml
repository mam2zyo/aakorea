# oci 프로필 전용 설정
# 기본적으로 application.yml의 설정을 상속받습니다.
app:
  # OCI 환경 데이터 저장 경로
  content:
    physical-root: ${AAKOREA_CONTENT_ROOT:/home/ubuntu/aakorea/contents}
  storage:
    location: ${AAKOREA_STORAGE_ROOT:/home/ubuntu/aakorea/uploads}
