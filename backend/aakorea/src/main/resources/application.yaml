spring:
  application:
    name: aakorea

  datasource:
    url: jdbc:postgresql://localhost:5432/aakorea
    username: servant
    password: 1234
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true