# local 프로필 전용 설정 (필요 시 아래에 추가)
# 기본적으로 application.yml의 설정을 상속받습니다.
app:
  auth:
    admin:
      # 필요 시 로컬 전용 계정 정보를 설정할 수 있습니다.
      username: ${AAKOREA_ADMIN_USERNAME}
      password: ${AAKOREA_ADMIN_PASSWORD}